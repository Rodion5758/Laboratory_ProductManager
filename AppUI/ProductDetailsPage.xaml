<UserControl x:Class="AppUI.ProductDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AppUI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel VerticalAlignment="Top" Margin="20">
        <TextBlock Text="Product details:" FontWeight="Bold" FontSize="15" Padding="10"/>
        
        <StackPanel VerticalAlignment="Top">
            <TextBlock>
                <Run Text="Name :"/>
                <Run Text="{Binding Product.Name, Mode=OneWay}"/>
            </TextBlock> 
                
            <TextBlock>
                <Run Text="Category :"/>
                <Run Text="{Binding Product.Category, Mode=OneWay}"/>
            </TextBlock> 
             <TextBlock>

                <Run Text="Price :"/>
                <Run Text="{Binding Product.Price, Mode=OneWay}"/>
            </TextBlock> 
                                
            <TextBlock>
                <Run Text="Quantity :"/>
                <Run Text="{Binding Product.Quantity, Mode=OneWay}"/>
            </TextBlock>

            <TextBlock>
                <Run Text="Total Cost :"/>
                <Run Text="{Binding Product.TotalCost, Mode=OneWay}"/>
            </TextBlock>
                                
            <TextBlock>
                <Run Text="Description :"/>
                <Run Text="{Binding Product.Description, Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>
        <Button Command="{Binding BackCommand}" Content="Back to products" HorizontalAlignment="Left"/>
    </StackPanel>
</UserControl>
